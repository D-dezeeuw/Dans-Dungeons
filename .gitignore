.DS_Store
.env

# Reference material for development (SRD PDFs, internal IP notes).
# Not for public distribution; keep local copies outside the repo.
docs/references/

# Build tooling — runtime vendor bundle is committed, not node_modules
node_modules/

# Transient Claude Code scheduler state
.claude/scheduled_tasks.lock

# Generated in-game exports (journals, world bibles) — produced at play time
docs/journals/
